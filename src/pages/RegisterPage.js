import React from 'react';
import Form from "../components/Form";

const RegisterPage = () => {
    return (
        <div className="page">
            <Form page={'Register'}/>
        </div>
    );
};

export default RegisterPage;