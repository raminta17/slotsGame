import React from 'react';
import Form from "../components/Form";

const LoginPage = () => {
    return (
        <div className="page">
            <Form page={'Login'}/>
        </div>
    );
};

export default LoginPage;